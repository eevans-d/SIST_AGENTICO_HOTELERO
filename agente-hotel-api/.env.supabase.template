# ============================================================================
# SUPABASE INTEGRATION - ENV TEMPLATE (do NOT commit with real secrets)
# Project: Agente Hotel API
# Generated: 2025-11-06
# ----------------------------------------------------------------------------
# USO:
# 1) Copia este archivo a `.env` o `.env.supabase`
# 2) Reemplaza TODOS los placeholders marcados con <> por tus valores reales
# 3) No compartas este archivo con secretos en repos públicos
# ============================================================================

# ---------------------------------------------------------------------------
# Supabase Database (Connection Pooling - Transaction Mode)
# IMPORTANTE: Usa SIEMPRE el pooler (puerto 6543) y SSL requerido
# Username debe incluir el project ref: postgres.<PROJECT_REF>
# ---------------------------------------------------------------------------
DATABASE_URL=postgresql://postgres.ofbsjfmnladfzbjmcxhx:<DB_PASSWORD>@aws-1-us-east-1.pooler.supabase.com:6543/postgres?sslmode=require

# Alternativa por componentes (settings.py construye la URL final si están completos)
POSTGRES_HOST=aws-1-us-east-1.pooler.supabase.com
POSTGRES_PORT=6543
POSTGRES_DB=postgres
POSTGRES_USER=postgres.ofbsjfmnladfzbjmcxhx
POSTGRES_PASSWORD=<DB_PASSWORD>
POSTGRES_POOL_SIZE=10
POSTGRES_MAX_OVERFLOW=10

# ---------------------------------------------------------------------------
# Supabase Project Info (para posible uso futuro de SDK, NO requerido por backend)
# ---------------------------------------------------------------------------
SUPABASE_PROJECT_URL=https://ofbsjfmnladfzbjmcxhx.supabase.co
SUPABASE_PROJECT_REF=ofbsjfmnladfzbjmcxhx
SUPABASE_ANON_KEY=<ANON_KEY>
SUPABASE_SERVICE_ROLE_KEY=<SERVICE_ROLE_KEY>

# ---------------------------------------------------------------------------
# Redis (sin cambios) - Reemplaza si usas Redis remoto con password
# ---------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379/0

# ---------------------------------------------------------------------------
# PMS (sin cambios por esta integración)
# ---------------------------------------------------------------------------
PMS_TYPE=mock
PMS_BASE_URL=http://localhost:8080

# ---------------------------------------------------------------------------
# Seguridad / App
# ---------------------------------------------------------------------------
ENVIRONMENT=development
DEBUG=true
SECRET_KEY=<CHANGE_ME_SECURE_RANDOM>
