# [PROMPT GA-01 - CORRECCIÓN] Configuración de Alertmanager (base)

global:
  resolve_timeout: 5m

route:
  receiver: "null"
  # Ejemplo de ruteo por severidad (descomentá y ajustá según necesidad)
  # routes:
  #   - matchers:
  #       - severity = critical
  #     receiver: slack
  #   - matchers:
  #       - severity = warning
  #     receiver: email

receivers:
  - name: "null"
  # Ejemplo Slack (Webhook):
  # - name: slack
  #   slack_configs:
  #     - api_url: ${SLACK_WEBHOOK_URL}
  #       channel: "#alertas-agente"
  #       send_resolved: true
  #       title: "[{{ .Status | toUpper }}] {{ .CommonLabels.alertname }}"
  #       text: >-
  #         {{ range .Alerts }}• {{ .Annotations.summary }}\n{{ .Annotations.description }}\n{{ end }}

  # Ejemplo Email (SMTP):
  # - name: email
  #   email_configs:
  #     - to: ${ALERT_EMAIL_TO}
  #       from: ${ALERT_EMAIL_FROM}
  #       smarthost: ${SMTP_HOST}:${SMTP_PORT}
  #       auth_username: ${SMTP_USER}
  #       auth_password: ${SMTP_PASSWORD}
  #       require_tls: true
