name: (DEPRECATED) Deploy to Fly.io

on:
  # Deshabilitado: Fly.io decommissioned. Mantener solo ejecución manual si se requiere auditoría puntual.
  workflow_dispatch: {}

concurrency:
  group: fly-deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Fly Deploy (disabled)
    runs-on: ubuntu-latest
    if: false
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flyctl (disabled)
        run: echo "flyctl disabled"

      - name: Set Fly Secrets (disabled)
        run: echo "skip"

      - name: Set Fly Secrets (disabled 2)
        run: echo "skip"

      - name: Readiness flags (disabled)
        run: echo "skip"

      - name: Deploy (disabled)
        run: echo "skip"

      - name: Smoke health check (disabled)
        run: echo "skip"
