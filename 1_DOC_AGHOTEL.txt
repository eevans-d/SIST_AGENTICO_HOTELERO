ÍNDICE ORIENTATIVO - AGENTE RECEPCIONISTA HOTELERO
Documentación Definitiva en 5 Partes (PMS-Céntrico)
PARTE 1 — ESTRATEGIA Y FUNDAMENTOS (PMS como Núcleo Central)
PARTE 2 — ARQUITECTURA PMS-CÉNTRICA Y STACK OPTIMIZADO
PARTE 3 — IMPLEMENTACIÓN TÉCNICA PROFUNDA (PMS-Integrada)
PARTE 4 — OPERACIÓN SIMPLIFICADA Y USABILIDAD MÁXIMA
PARTE 5 — DESPLIEGUE ACELERADO Y EVOLUCIÓN CONTROLADA
PARTE 1/5 — ESTRATEGIA Y FUNDAMENTOS (PMS COMO NÚCLEO CENTRAL)
1.1 VISIÓN Y PROPÓSITO DEL AGENTE RECEPCIONISTA
Objetivo Central
Implementar un Agente Recepcionista Hotelero inteligente 24/7 que opere exclusivamente a través del PMS como núcleo central, automatizando la atención por WhatsApp (texto/audio) y Gmail, maximizando el impacto operativo mientras minimiza el desarrollo al reutilizar las capacidades existentes del PMS.
Filosofía Fundamental: PMS como "La Central"
El PMS no es solo un sistema de soporte, sino el cerebro operativo del hotel. El agente actúa como una capa de experiencia conversacional inteligente que:
Consulta al PMS para disponibilidad y tarifas
Verifica información en tiempo real
Confirma reservas directamente en el PMS
Responde basándose en la información del PMS
Esta arquitectura reduce dramáticamente el tiempo de desarrollo al evitar duplicar funcionalidades que el PMS ya incorpora (inventario, tarifas, reservas, clientes, facturación).
1.2 ALCANCE TÉCNICO DEFINITIVO
Canales de Comunicación (Exclusivos)
WhatsApp Business
Texto: Consultas, reservas, confirmaciones
Audio: STT (Whisper) para consultas habladas + TTS (Coqui/eSpeak-NG) para respuestas
Multimedia: Recepción de comprobantes de pago/transferencias
Horario: 24/7 automatizado con escalamiento inteligente al staff
Gmail Integration
IMAP polling o Gmail API para consultas por email
Respuestas automáticas con plantillas profesionales
Adjuntos: Vouchers, confirmaciones, información del hotel
Threading: Conversaciones organizadas por cliente
PMS como Fuente Única de Verdad
QloApps (Recomendación Principal)
Sistema completo open source con API REST documentada
Manejo integral: inventario, tarifas, reservas, clientes, facturación
Comunidad activa (2K+ GitHub stars)
Ecosistema de plugins y extensiones
HotelDruid (Alternativa Ligera)
Ideal para hoteles pequeños-medianos
Setup más simple, API básica pero funcional
Licencia GPL completamente libre
Capacidades de Audio Integradas
STT (Speech-to-Text)
Whisper (OpenAI): Modelo "base" para español, procesamiento local
Formatos soportados: OGG (WhatsApp), MP3, WAV
Pipeline: Audio WhatsApp → FFmpeg conversión → Whisper → Texto
TTS (Text-to-Speech)
Coqui TTS: Voces naturales en español (opción calidad)
eSpeak-NG: Alternativa ligera para recursos limitados
Cache inteligente: Respuestas frecuentes pre-generadas
Idioma y Contexto Profesional
Español profesional hotelero con terminología específica del sector
Políticas y reglamentos hoteleros argentinos cuando sea relevante
Tono cordial pero profesional, evitando regionalismos excesivos
Claridad universal manteniendo naturalidad en la comunicación
1.3 ESTRATEGIA DE REDUCCIÓN DE DESARROLLO
Principio: "PMS Maneja, Agente Comunica"
Lo que HACE el PMS (QloApps/HotelDruid):
✅ Gestión completa de inventario (habitaciones, tipos, estados)
✅ Motor de tarifas dinámicas y promociones
✅ Sistema completo de reservas (crear, modificar, cancelar)
✅ Base de datos de clientes con historial
✅ Facturación y reportes (condicional)
✅ Dashboard administrativo hotelero
✅ Control de ocupación y disponibilidad en tiempo real
Lo que HACE el Agente:
🤖 Interfaz conversacional WhatsApp/Gmail (texto/audio)
🤖 Procesamiento de lenguaje natural (intents hoteleros específicos)
🤖 Pipeline de audio (STT/TTS optimizado)
🤖 Automatización de respuestas basadas en datos del PMS
🤖 Escalamiento inteligente al staff cuando necesario
🤖 Recordatorios automáticos (pre/post estancia)
Flujo de Integración Típico
Cliente WhatsApp: "¿Hay disponibilidad 15-17 dic para 2 personas?"
↓
Agente NLP → Intent: disponibilidad | Entidades: fechas, huéspedes
↓
Consulta PMS API → GET /availability?checkin=2024-12-15&checkout=2024-12-17&guests=2
↓
PMS responde → [{room_type: "doble", price: 12000, available: true}, ...]
↓
Agente genera → "Sí, habitación doble disponible $12,000/noche. Total $24,000. ¿Reservamos?"
1.4 PRINCIPIOS PARA DESARROLLADOR ÚNICO
Modularidad y Reutilización Extrema: Componentes independientes (PMS Adapter, NLP Engine, Audio Pipeline, Message Gateway) con interfaces claras.
Herramientas Comunitarias que Aceleran Desarrollo: PMS pre-construido, templates de integración, workflows pre-definidos, stacks Docker.
Automatización desde Día 1: Scripts de despliegue, backups automáticos, monitoreo básico, tests esenciales.
Configuración Declarativa: Variables de entorno, templates modificables, reglas en YAML/JSON para fácil replicación.
1.5 KPIs DE IMPACTO MEDIBLE
Eficiencia Operativa
Tiempo de respuesta: < 2 minutos (objetivo: < 30 segundos)
Precisión de disponibilidad: > 98%
Reducción carga staff: > 70%
Cobertura horaria: 24/7
Conversión y Revenue
Tasa de conversión consulta → reserva: > 25%
Tiempo consulta → reserva: < 10 minutos
Upselling automático: Sugerencias de habitaciones superiores
Reducción no-shows: Recordatorios automáticos
Calidad de Servicio
Satisfacción del cliente: NPS > 8/10
Precisión de respuestas: > 95%
Escalamiento efectivo: < 5% conversaciones requieren intervención
Consistencia: 100%
Métricas Técnicas
Uptime del sistema: > 99.5%
Latencia de respuesta: < 3s texto, < 10s audio
Precisión STT: > 90%
Sincronización PMS: < 1 segundo
1.6 DECISIONES CRÍTICAS FUNDAMENTADAS
Facturación AFIP (Decisión Condicional)
Criterio de Evaluación:
Si complejidad ≤ MEDIA: Implementar usando capacidades nativas del PMS.
Si complejidad > MEDIA: Posponer para temporada de verano.
Alternativa Temporal: PMS maneja facturación manual, agente genera pre-facturas.
Selección de Canales (WhatsApp + Gmail Únicamente)
Justificación Estratégica: Concentración de esfuerzos, simplicidad de desarrollo, cobertura efectiva.
Arquitectura PMS-Céntrica vs Agente Autónomo
Ventajas: Reducción 60-80% tiempo desarrollo, consistencia de datos, menor superficie de error, mantenimiento simplificado.
1.7 RECURSOS COMUNITARIOS IDENTIFICADOS
PMS Open Source Seleccionados
QloApps:
GitHub: https://github.com/webkul/qloapps (2K+ stars)
API: REST completa y documentada
HotelDruid:
Sitio: hoteldruid.com
Licencia: GPL completamente libre
Frameworks y Templates Identificados
Integración WhatsApp Business: Templates pre-aprobados, librerías Python.
FastAPI + Rasa Integration: Repositorios GitHub con ejemplos de chatbots hoteleros.
Docker Compose Stacks: Configuraciones completas (PMS + DB + Proxy), scripts de backup, SSL automático.
1.8 ARQUITECTURA DE ALTO NIVEL
Componentes Principales
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   WHATSAPP      │    │     GMAIL        │    │   DASHBOARD     │
│ (Webhook/API)   │    │   (IMAP/API)     │    │    STAFF        │
└─────────┬───────┘    └─────────┬────────┘    └─────────┬───────┘
          │                      │                       │
          └──────────┬───────────┴───────────────────────┘
                     │
          ┌──────────▼───────────┐
          │   MESSAGE GATEWAY    │
          │  (Normalización)     │
          └──────────┬───────────┘
                     │
          ┌──────────▼───────────┐
          │    ORQUESTADOR       │
          │   (FastAPI Core)     │
          └─────┬──────────┬─────┘
                │          │
    ┌───────────▼─┐      ┌─▼────────────┐
    │ NLP ENGINE  │      │ AUDIO ENGINE │
    │(Rasa+spaCy) │      │(Whisper+TTS) │
    └───────────┬─┘      └─┬────────────┘
                │          │
          ┌─────▼──────────▼─────┐
          │  BUSINESS LOGIC      │
          │ (Reglas Hoteleras)   │
          └─────────┬────────────┘
                    │
          ┌─────────▼────────────┐
          │   PMS ADAPTER        │
          │(QloApps/HotelDruid)  │
          └─────────┬────────────┘
                    │
          ┌─────────▼────────────┐
          │       PMS            │
          │ (Fuente de Verdad)   │
          └──────────────────────┘


Flujo de Datos End-to-End
Entrada: Mensaje WhatsApp/Gmail → Gateway normalización
Procesamiento: NLP extrae intent/entidades → Business Logic aplica reglas
Consulta: PMS Adapter consulta/modifica datos en PMS
Respuesta: Generación respuesta → Envío por canal apropiado
Logging: Registro completo
1.9 CRITERIOS DE ÉXITO Y GO/NO-GO
Criterios GO (Listo para Producción)
✅ Canales operativos: WhatsApp + Gmail sin errores.
✅ Integración PMS: Sincronización >98% precisión.
✅ Audio funcional: >80% transcripciones útiles.
✅ KPIs mínimos: Tiempo respuesta <2 min, uptime >99%.
✅ Staff autónomo: Opera dashboard sin asistencia.
Criterios NO-GO (Requiere Corrección)
❌ Inconsistencias PMS: >1% discrepancias.
❌ Double-booking: Cualquier caso.
❌ Fallos canal persistentes: >30 min inaccesibles.
❌ Latencia sistemática: Fuera de objetivo.
❌ Staff no puede operar: Requiere asistencia.
1.10 PRÓXIMOS PASOS Y PREPARACIÓN PARTE 2
Decisiones Inmediatas Requeridas
Selección PMS definitiva: QloApps vs HotelDruid (recomendación: QloApps)
WhatsApp API: Twilio vs Meta Cloud
Gmail: IMAP polling vs Gmail API
Hosting: VPS dedicado vs cloud managed
Preparación Técnica
Instalación PMS: Setup inicial QloApps
Entorno desarrollo: Docker, Git, Python 3.11+
Cuentas desarrollador: Meta Business, Google Cloud
Migración datos: Preparar CSV
PARTE 2/5 — ARQUITECTURA PMS-CÉNTRICA Y STACK TECNOLÓGICO OPTIMIZADO
2.1 ARQUITECTURA DE INTEGRACIÓN TOTAL CON PMS
Principio Fundamental: PMS como "La Central Operativa"
La arquitectura se fundamenta en el concepto de "PMS como Single Source of Truth". QloApps/HotelDruid actúa como el cerebro operativo, y el agente funciona como una capa de experiencia conversacional.
El PMS maneja TODA la lógica de negocio hotelera:
✅ Inventario, Motor de tarifas, Sistema de reservas, Base de datos clientes, Facturación, Reportes.
El Agente maneja SOLO la comunicación automatizada:
🤖 Canales multimodales, Procesamiento NLP, Pipeline de audio, Automatización conversacional, Escalamiento dinámico.
Flujo Operacional End-to-End
┌─────────────────┐         ┌─────────────────┐
│   WHATSAPP      │         │     GMAIL       │
│ (Audio/Texto)   │         │   (IMAP/API)    │
└─────────┬───────┘         └─────────┬───────┘
          │                           │
          └─────────┬───────────────────┘
                    │
         ┌──────────▼──────────┐
         │  MESSAGE GATEWAY    │ ← Normalización unificada
         │ • Rate Limiting     │
         │ • Auth/Validation   │
         └──────────┬──────────┘
                    │
         ┌──────────▼──────────┐
         │ ORQUESTADOR FASTAPI │ ← Coordinación central
         │ • NLP Engine        │
         │ • Audio Pipeline    │
         │ • Business Rules    │
         │ • Session Manager   │
         └──────────┬──────────┘
                    │
         ┌──────────▼──────────┐
         │   PMS ADAPTER       │ ← Capa de abstracción
         │ • API Integration   │
         │ • Cache Layer       │
         │ • Failover Logic    │
         └──────────┬──────────┘
                    │
         ┌──────────▼──────────┐
         │  QLOAPPS (PMS)      │ ← Fuente única de verdad
         │ • Inventory         │
         │ • Reservations      │
         │ • Customers         │
         │ • Billing           │
         └─────────────────────┘


2.2 STACK TECNOLÓGICO MINIMALISTA PARA DESARROLLADOR ÚNICO
PMS: QloApps como Núcleo Central
Justificación: API REST completa, comunidad activa, instalación Docker, ecosistema de plugins.
Setup QloApps Optimizado:
# docker-compose.qloapps.yml
version: '3.8'
services:
  qloapps:
    image: webkul/qloapps:latest
    environment:
      DB_SERVER: mysql
      DB_NAME: qloapps
      DB_USER: qloapps
      DB_PASSWD: ${MYSQL_PASSWORD}
    ports:
      - "8080:80"
    depends_on:
      - mysql
    volumes:
      - qloapps_data:/var/www/html

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: qloapps
      MYSQL_USER: qloapps
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  qloapps_data:
  mysql_data:


Backend Agente: FastAPI + Arquitectura Modular
Estructura del Proyecto Optimizada:
agente-hotel/
├── app/
│   ├── core/
│   ├── services/
│   ├── models/
│   ├── routers/
│   └── main.py
├── docker-compose.yml
├── requirements.txt
└── .env


FastAPI Core Implementation:
# app/main.py
from fastapi import FastAPI
from .routers import webhooks, admin
from .services import pms_adapter, message_gateway

app = FastAPI(title="Agente Recepcionista Hotelero", version="1.0.0")

app.include_router(webhooks.router, prefix="/webhooks", tags=["webhooks"])
app.include_router(admin.router, prefix="/admin", tags=["admin"])

@app.on_event("startup")
async def startup():
    await pms_adapter.initialize()
    await message_gateway.start()

@app.on_event("shutdown")
async def shutdown():
    await pms_adapter.close()


NLP Engine: Rasa + spaCy Especializado
Configuración Rasa Hotelera:
# config.yml - Pipeline optimizado español
language: es
pipeline:
  - name: WhitespaceTokenizer
  - name: RegexFeaturizer
  - name: LexicalSyntacticFeaturizer
  - name: CountVectorsFeaturizer
  - name: DIETClassifier
    epochs: 100
  - name: EntitySynonymMapper
policies:
  - name: MemoizationPolicy
  - name: RulePolicy
  - name: UnexpecTEDIntentPolicy
    max_history: 5
    epochs: 100


Intents Hoteleros Específicos:
# domain.yml
intents:
  - check_availability
  - get_prices
  - make_reservation
  - modify_reservation
  - ask_services
  - ask_policies
entities:
  - check_in_date
  - check_out_date
  - num_guests
  - room_type


Audio Pipeline: Whisper + TTS Optimizado
STT Implementation:
# services/audio_pipeline.py
import whisper
import asyncio
from pathlib import Path

class AudioProcessor:
    def __init__(self):
        self.whisper_model = whisper.load_model("base")

    async def transcribe_whatsapp_audio(self, audio_url: str) -> dict:
        try:
            audio_file = await self._download_audio(audio_url)
            wav_file = await self._convert_to_wav(audio_file)
            result = await asyncio.to_thread(
                self.whisper_model.transcribe, str(wav_file), language="es"
            )
            # Cleanup
            return {"success": True, "text": result["text"].strip()}
        except Exception as e:
            return {"success": False, "error": str(e)}

    async def _convert_to_wav(self, input_file: Path) -> Path:
        output_file = input_file.with_suffix('.wav')
        process = await asyncio.create_subprocess_exec(
            'ffmpeg', '-i', str(input_file), '-ar', '16000', '-ac', '1', str(output_file),
            stdout=asyncio.subprocess.DEVNULL, stderr=asyncio.subprocess.PIPE
        )
        await process.communicate()
        return output_file


2.3 INTEGRACIÓN BIDIRECCIONAL CON QLOAPPS
PMS Adapter: Capa de Abstracción Robusta
# services/pms_adapter.py
import httpx
from ..core.redis_client import redis_client

class QloAppsAdapter:
    def __init__(self):
        self.base_url = settings.PMS_BASE_URL
        self.api_key = settings.PMS_API_KEY
        self.session = None
        self.cache_ttl = 300  # 5 minutos

    async def initialize(self):
        self.session = httpx.AsyncClient(
            base_url=self.base_url,
            headers={"Authorization": f"Bearer {self.api_key}"},
            timeout=30.0
        )

    async def check_availability(self, check_in, check_out, guests=1, room_type=None) -> list:
        cache_key = f"availability:{check_in}:{check_out}:{guests}:{room_type or 'any'}"
        cached_data = await redis_client.get(cache_key)
        if cached_data:
            return json.loads(cached_data)

        # ... (lógica de consulta al PMS) ...
        
        await redis_client.setex(cache_key, self.cache_ttl, json.dumps(data))
        return data

    async def create_reservation(self, reservation_data: dict) -> dict:
        # ... (lógica de creación y validación) ...
        await self._invalidate_availability_cache(
            reservation_data["check_in"], reservation_data["check_out"]
        )
        return {"success": True, "data": booking_data}

    async def _invalidate_availability_cache(self, check_in, check_out):
        pattern = f"availability:{check_in}:*"
        keys = await redis_client.keys(pattern)
        if keys:
            await redis_client.delete(*keys)


Sistema de Tolerancia a Fallos
# services/fallback_manager.py
from enum import Enum

class SystemStatus(Enum):
    HEALTHY = "healthy"
    DEGRADED = "degraded"
    CRITICAL = "critical"

class FallbackManager:
    def __init__(self):
        self.status = SystemStatus.HEALTHY
        self.fallback_responses = {
            "availability": "Estoy consultando disponibilidad. Te respondo en unos minutos.",
            # ... más respuestas
        }

    async def activate_degraded_mode(self, reason: str):
        self.status = SystemStatus.DEGRADED
        # Notificar staff, usar cache extendido, etc.

    async def get_fallback_response(self, intent: str) -> str:
        # Lógica para devolver respuesta de contingencia
        return self.fallback_responses.get(intent, self.fallback_responses["general"])


2.4 IMPLEMENTACIÓN DE CANALES (WHATSAPP + GMAIL)
WhatsApp Integration: Meta Cloud API vs Twilio
Recomendación: Meta Cloud API (Costo-Efectivo)
Implementación WhatsApp Meta Cloud:
# services/whatsapp_client.py
import httpx

class WhatsAppMetaClient:
    def __init__(self):
        self.base_url = "[https://graph.facebook.com/v18.0](https://graph.facebook.com/v18.0)"
        self.access_token = settings.WHATSAPP_ACCESS_TOKEN
        self.phone_number_id = settings.WHATSAPP_PHONE_NUMBER_ID
    
    async def send_message(self, to: str, message: str) -> bool:
        payload = {"messaging_product": "whatsapp", "to": to, "text": {"body": message}}
        async with httpx.AsyncClient() as client:
            response = await client.post(
                f"{self.base_url}/{self.phone_number_id}/messages",
                json=payload, headers={"Authorization": f"Bearer {self.access_token}"}
            )
            return response.status_code == 200

    def parse_webhook(self, payload: dict) -> list:
        # Lógica para extraer mensajes del webhook de Meta
        pass


Gmail Integration: IMAP Polling (Simplicidad)
Justificación: Configuración simple, sin quotas, protocolo estándar.
# services/gmail_client.py
import imaplib
import email

class GmailIMAPClient:
    def __init__(self):
        self.imap_server = "imap.gmail.com"
        self.username = settings.GMAIL_USERNAME
        self.password = settings.GMAIL_APP_PASSWORD
        self.connection = None

    async def poll_new_messages(self) -> list:
        if not self.connection:
            await self.connect()
        
        try:
            status, messages = self.connection.search(None, "UNSEEN")
            new_messages = []
            # ... (lógica para leer y parsear emails) ...
            return new_messages
        except Exception as e:
            self.connection = None # Forzar reconexión
            return []

    async def send_response(self, to: str, subject: str, body: str):
        # ... (lógica para enviar emails con SMTPlib) ...
        pass


2.5 INFRAESTRUCTURA Y CONFIGURACIÓN COMPLETA
Docker Compose Stack Completo
# docker-compose.yml - Stack completo Agente + PMS
version: '3.8'
services:
  # PMS QloApps
  qloapps:
    # ... (configuración de QloApps)
  mysql:
    # ... (configuración de MySQL)

  # Agente Stack
  agente-api:
    build: .
    environment:
      # ... (variables de entorno para DB, Redis, PMS, WhatsApp, Gmail)
    ports:
      - "8000:8000"
    depends_on: [postgres, redis, qloapps]
  
  postgres:
    # ... (configuración de PostgreSQL)
  redis:
    # ... (configuración de Redis)

  # Reverse Proxy
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl
    depends_on: [agente-api, qloapps]


Configuración NGINX Optimizada
# nginx/nginx.conf
events { worker_connections 1024; }
http {
    upstream agente_api { server agente-api:8000; }
    upstream qloapps { server qloapps:80; }
    
    # Rate limiting
    limit_req_zone $binary_remote_addr zone=webhooks:10m rate=30r/m;
    
    server {
        listen 443 ssl;
        server_name tu-dominio.com;
        
        ssl_certificate /etc/nginx/ssl/cert.pem;
        ssl_certificate_key /etc/nginx/ssl/key.pem;
        
        location /webhooks/ {
            limit_req zone=webhooks burst=10 nodelay;
            proxy_pass http://agente_api;
            # ... (headers)
        }
        
        location /pms/ {
            proxy_pass http://qloapps/;
            # ... (headers)
        }
    }
}


2.6 RECURSOS COMUNITARIOS Y HERRAMIENTAS ESPECÍFICAS
Repositorios GitHub Identificados
QloApps: webkul/qloapps, webkul/qloapps-docker
WhatsApp: python-whatsapp-business, Templates en Meta Business Manager
Rasa: RasaHQ/rasa-hotel-assistant
Scripts de Automatización
Backup Automatizado:
#!/bin/bash
# scripts/backup.sh
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/backups"

# Backup MySQL (QloApps)
docker exec mysql mysqldump -u root -p${MYSQL_ROOT_PASSWORD} qloapps > $BACKUP_DIR/qloapps_$DATE.sql

# Backup PostgreSQL (Agente)
docker exec postgres pg_dump -U ${POSTGRES_USER} ${POSTGRES_DB} > $BACKUP_DIR/agente_$DATE.sql

echo "Backup completado"


Variables de Entorno Template
# .env.template
# PMS QloApps
MYSQL_ROOT_PASSWORD=
MYSQL_PASSWORD=
PMS_API_KEY=

# Agente Database
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=
REDIS_PASSWORD=

# WhatsApp Meta Cloud
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_VERIFY_TOKEN=

# Gmail
GMAIL_USERNAME=
GMAIL_APP_PASSWORD=

# Security
SECRET_KEY=


2.7 DECISIONES CRÍTICAS Y PRÓXIMOS PASOS
Decisiones Tomadas para Eficiencia:
WhatsApp: Meta Cloud API
Gmail: IMAP polling
NLP: Híbrido regex+spaCy inicial, Rasa cuando sea necesario
TTS: eSpeak-NG por defecto
Hosting: VPS dedicado inicial
Preparación para PARTE 3:
Setup Inmediato Requerido: Instalación QloApps, Cuentas desarrollador, VPS provisioning, Repositorio Git.
Aspectos a Profundizar en PARTE 3: Implementación NLP, flujos de reserva, reglas de negocio, dashboard staff.